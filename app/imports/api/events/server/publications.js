import {Meteor} from 'meteor/meteor';
import {Events} from '../events.js';

//Meteor.publish('events', session => {
//check(session, String);
//if (!session) {
//return this.ready();
//} else {
//return Events.find({session: session});
//}
//});
