// storagePath.js

// Google Cloud Storage Bucket Name
export const bucketName = Meteor.isProduction ? "slidespecs" : "slidespecs-dev";

// Place for saving files locally.
export const storagePath = "/Users/jwrnr/Documents/slidespecs-db";
